# Database Configuration
DATABASE_URL=postgres://username:password@localhost:5432/ordo_db?sslmode=disable

# Electric SQL Configuration
SOURCE_ID=your_electric_sql_source_id
SOURCE_SECRET=your_electric_sql_secret_jwt_token

# Server Configuration
PORT=8080
BASE_URL=http://localhost:8080

# Google OAuth Configuration (REQUIRED)
# Get these from Google Cloud Console: https://console.cloud.google.com/
# 1. Create a new project or select existing one
# 2. Enable Google+ API
# 3. Create OAuth 2.0 credentials (Web Application)
# 4. Add authorized redirect URIs: http://localhost:8080/auth/google/callback
GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
GOOGLE_SECRET=your_google_client_secret

# Session Configuration (REQUIRED)
# Generate a secure random string for session encryption
# You can use: openssl rand -base64 32
SESSION_SECRET=your_secure_random_session_secret_key_here

# Frontend URL (for CORS and redirects)
FRONTEND_URL=http://localhost:5173
